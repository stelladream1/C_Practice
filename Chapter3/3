#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
int main() {
	double base=0;
	double height = 0;

	printf("삼각형의 밑변:");
	scanf("%lf", &base);

	printf("삼각형의 높이:");
	scanf("%lf", &height);

	double area = base * height * 0.5;
	printf("삼각형의 높이:%lf", area);
	
}
