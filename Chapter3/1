#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
int main() {
	int x=0, y=0, z=0;
	float sum=0, avg=0;

	printf("실수 입력:");
	scanf("%d", &x);

	printf("실수 입력:");
	scanf("%d", &y);

	printf("실수 입력:");
	scanf("%d", &z);
	sum = x + y + z;
	avg = sum / 3;
	printf("합:%f, 평균:%f", sum, avg);
	
}
